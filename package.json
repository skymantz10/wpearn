{
  "name": "wordpress-redirects",
  "version": "1.0.0",
  "description": "Static HTML redirects for WordPress posts with Open Graph previews",
  "private": true,
  "scripts": {
    "build": "node scripts/generate-redirects.js"
  }
}
